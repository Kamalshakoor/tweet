<!-- app/views/layouts/_navbar.html.erb -->

<nav class="navbar navbar-expand-lg navbar-info bg-info">
  <div class="container">
    <!-- Navbar Brand (Left) -->
    <%= link_to 'Tweet', root_path,class: "navbar-brand text-white" %>

    <!-- Navbar Toggler (for responsive design) -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Buttons (Right) -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to 'Logout', destroy_user_session_path	, class:'btn btn-outline-primary text-white', method: :delete %>
        </li>
      <% else %>
          <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class:'btn btn-outline-success text-white' %>      
          </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>








